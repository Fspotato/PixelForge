services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
    container_name: pixelforge-prod-web
    env_file:
      - ../backend/env/.env.prod
    environment:
      DJANGO_ENV: prod
      PYTHONPATH: /app
    command: >
      sh -c "python manage.py migrate &&
      gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3"
    volumes:
      - ../backend/logs:/app/logs
      - ../scripts:/scripts:ro
    ports:
      - "8020:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/system/ping/').read()\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  celery-worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
    container_name: pixelforge-prod-celery-worker
    env_file:
      - ../backend/env/.env.prod
    environment:
      DJANGO_ENV: prod
      PYTHONPATH: /app
    command: celery -A config worker --loglevel=info
    volumes:
      - ../backend/logs:/app/logs
      - ../scripts:/scripts:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery-beat:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
    container_name: pixelforge-prod-celery-beat
    env_file:
      - ../backend/env/.env.prod
    environment:
      DJANGO_ENV: prod
      PYTHONPATH: /app
    command: >
      sh -c "celery -A config beat --loglevel=info"
    volumes:
      - ../backend/logs:/app/logs
      - ../scripts:/scripts:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: pgvector/pgvector:pg17
    container_name: pixelforge-prod-postgres
    hostname: prod-postgres
    env_file:
      - ../backend/env/.env.prod
    ports:
      - "5434:5432"
    volumes:
      - prod_postgres_data:/var/lib/postgresql/data
      - ../scripts/init-pgvector.sql:/docker-entrypoint-initdb.d/init-pgvector.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:8-alpine
    container_name: pixelforge-prod-redis
    hostname: prod-redis
    ports:
      - "6381:6379"
    volumes:
      - prod_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  prod_postgres_data:
  prod_redis_data:
